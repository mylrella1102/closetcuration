<%= render "shared/sidebar" %>
<div class="bg-light border rounded p-4 mx-auto">
  <div class = "d-flex flex-column text-center">
    <div class = "post-img">
      <%= image_tag @item.image.variant(resize: '200x200') %> 
    </div>
    <div class = "mt-3 mb-3 ">
        名前：<%= @item.name %><br>
        カテゴリー：<%= @item.category.name %><br>
        色：<%= @item.color.name %><br>
        季節：<%= @item.season.name %></p>
      <% if @item.info.present? %>
        <span>メモ：<%= @item.info %></span>
      <% end %>
    </div>
    <%# <div class='star-btn'>
      <%= image_tag "star.png", class:"star-icon me-2 " %>
      <%# <span class='star-count'>0</span><br>
    </div> %> 
    <div class="row mt-3 justify-content-center">
      <%= link_to "編集", edit_item_path(@item.id), method: :get, class: "btn btn-primary col-sm-5 ms-1" %>
      <%= link_to "削除", item_path(@item.id), method: :delete, class: "btn btn-danger col-sm-5 ms-1" %>
    </div>
</div>