<%= render "shared/sidebar" %>
<div class= "d-flex justify-content-center col-12 w-25">
<%= form_with model: @post, url: posts_path, local: true, class: "p-5", id: "step1-form" do |f| %>
  <div class="post-box">
    <p class="text-center mb-3 fs-2">コーディネート登録</p>

    <div class="add-form row p-3">
    <div class="d-flex justify-content-center flex-column">
      <% Account.all.each do |account| %>
        <label class="form-radio mb-2 text-center">
          <input class="form-radio-input" type="radio" name="post[account_id]" value="<%= account.id %>">
          <% if account.image.present? %>
            <%= image_tag account.image.variant(resize: '50x50'), class: "rounded-circle" %>
          <% else %>
            <%= image_tag "icon.png", variant: { resize: '50x50' }, class: "icon-resize rounded-circle" %>
          <% end %>
          <%= account.name %>
        </label>
      <% end %>
    </div>
    <%= hidden_field_tag 'post[account_id]', params[:account_id] %>
    <p class="text-center next">
      <%= button_tag "次へ", class: "btn btn-primary", type: "button", id: "next" %>
    </p>
    </div>
  </div>
<% end %>
</div>

    <div id="step2-form-container" class="d-flex justify-content-center col-12 w-25" >
      <!-- ステップ2のフォームがここに表示されます -->
    </div>